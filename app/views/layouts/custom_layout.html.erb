<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : "Default Title" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>

<body>
  <header>
    <h1>My Custom Layout 🚀</h1>
    <nav>
      <a href="/">Inicio</a> |
      <a href="/custom">Página Personalizada</a>
    </nav>
  </header>

  <div class="container">
    <aside style="float:left; width:20%; background:#f0f0f0; padding:10px;">
      <h3>Sidebar</h3>
      <%= content_for?(:sidebar) ? yield(:sidebar) : "Contenido por defecto del Sidebar" %>
    </aside>

    <main style="float:right; width:75%; padding:10px;">
      <!-- Aquí se renderiza el contenido principal -->
      <%= yield %>
    </main>
  </div>

  <footer style="clear:both; margin-top:20px;">
    <p>© 2025 MyApp. Todos los derechos reservados.</p>
  </footer>
</body>
</html>
